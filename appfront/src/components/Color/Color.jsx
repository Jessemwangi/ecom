import React from 'react';
import './Color.scss'

const Color = ({color}) => {
    return (
        <div className="color" style={{background:color}}>
    
        </div>
    );
};

export default Color;